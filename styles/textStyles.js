import { StyleSheet } from "react-native";

const textStyles = StyleSheet.create({
  header: {
    fontSize: 24,
    fontWeight: "bold",
    marginVertical: 20,
  },
  labelText: {
    fontSize: 18,
    marginTop: 20,
    fontWeight: "bold",
  },
  text: {
    fontSize: 15,
    marginVertical: 15,
  }
});

export default textStyles;